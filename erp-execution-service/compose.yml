# Same stack as docker-compose.yml (Compose v2 also looks for compose.yml).

services:
  erp-execution-service:
    build: .
    ports:
      - "${PORT}:8790"
    env_file:
      - ${ENV_FILE:-.env}
    restart: unless-stopped
