# Same stack — some Dokploy / Compose setups default to compose.yaml first.

services:
  erp-execution-service:
    build: .
    ports:
      - "${PORT}:8790"
    env_file:
      - ${ENV_FILE:-.env}
    restart: unless-stopped
