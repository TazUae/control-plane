# All runtime configuration comes from `.env` (see `.env.example`).

services:
  erp-execution-service:
    build: .
    ports:
      - "${PORT}:8790"
    env_file:
      - .env
    restart: unless-stopped
