node_modules/
dist/
tmp/
*.log
erp-worker/node_modules/
erp-worker/*.log

# Local env (never commit). Copy from `.env.example` for development.
.env
.env.local
.env.production
.env.secrets
# Catch-all for other dotenv variants (e.g. .env.bak.YYYYMMDD) and backup files;
# keep checked-in example templates.
.env.*
!.env*.example
*.bak
